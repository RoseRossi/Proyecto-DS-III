services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - app

  # app:
  #   image: tu_imagen_de_go:latest
  #   ports:
  #     - "4700:4700"
